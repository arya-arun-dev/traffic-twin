# Required by the browser for the MapTiler basemap and geocoding.
VITE_MAPTILER_API_KEY=replace-with-your-maptiler-key

# Local-only PostgreSQL password used by Docker Compose.
POSTGRES_PASSWORD=replace-with-a-local-password
